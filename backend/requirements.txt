# Backend - Python Dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 (its last release, effectively unmaintained) probes the
# bcrypt backend with an internal self-test password that trips the
# stricter 72-byte hard-error bcrypt added in 4.1+ (previously it silently
# truncated). Without this pin, a fresh install grabs the latest bcrypt and
# every password hash/verify call — including signup and login — raises
# ValueError: password cannot be longer than 72 bytes.
bcrypt<4.1.0
python-multipart>=0.0.20
google-generativeai>=0.8.0
elevenlabs>=1.0.0
livekit-api>=0.8.0
gTTS>=2.5.0
twilio>=8.0.0
slowapi>=0.1.9
arq>=0.25.0
redis>=5.0.0
