# Dev/test dependencies. Install with:  pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
# For async endpoint/service tests as they get added.
pytest-asyncio>=0.23.0
# FastAPI's TestClient transport.
httpx>=0.27.0
